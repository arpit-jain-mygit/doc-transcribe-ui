# Doc Transcribe UI (Static)

This project is now a static web app.

## Kept structure

- `index.html`
- `css/`
- `js/`
- `partials/`

## Run locally

```bash
npm run start
```

Then open `http://localhost:4200/`.
